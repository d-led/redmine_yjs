# Gemfile for redmine_yjs plugin
# Dependencies required for WebSocket proxy mode (ActionCable)

source 'https://rubygems.org'

group :production, :development do
  # WebSocket client for ActionCable proxy mode
  # Only needed when websocket_proxy is enabled
  gem 'websocket-client-simple', '~> 0.3.0', require: false
end

