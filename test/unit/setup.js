/**
 * Vitest setup file for JavaScript unit tests
 * 
 * Note: This setup is for pure logic testing. DOM interactions
 * are tested in E2E tests using Playwright (see test/e2e/).
 */

import { vi } from 'vitest';

// Minimal mocks for pure logic tests
// DOM testing is done in E2E tests with Playwright

